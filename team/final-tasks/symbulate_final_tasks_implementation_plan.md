# Symbulate `final-tasks` — Implementation Plan

Generated 2026-08-09. Synthesized from every file in `team/final-tasks/` (audit,
distributions, graphics, summarize-and-misc, and the hierarchical-models guide)
plus `team/fast_sim_notes.md` / `team/random_seed_notes.md` (identical
duplicates of the copies inside `team/final-tasks/audit/`).

**This document was not tested against the codebase — no `pytest` runs, no
patch applications, no code changes were made while producing it.** Line
numbers and "confirmed non-conflicting" claims below come from the source
documents and from dry-run `git apply --check` calls (which touch no files),
not from applying and running anything. Treat every "confirmed" claim in this
document as "confirmed by document inspection / dry-run diff check," not "we
ran the tests."

Total scope: **34 discrete implementation tasks** across 5 workstreams. 15 of
the 25 audit findings have no patch in this batch (11, 13, 15, 17, 19–21,
23–25) and are out of scope unless separately commissioned.

---

## How to read this document

1. **Workstream map** — which files each workstream touches, and where they overlap.
2. **Phased plan** — the order to implement/merge in, chosen to minimize merge
   conflicts and to land low-risk fixes before high-churn feature work.
3. **Task detail** — one entry per task: what it does, the Claude Code prompt
   to use, what needs manual testing, and pitfalls.
4. **Open team decisions** — five places where a patch author explicitly left
   a product decision unmade; don't let Claude Code decide these unilaterally.
5. **Testing gaps** — things no patch in this batch tests, that a human should
   plan for separately.

A companion checklist spreadsheet (`symbulate_final_tasks_checklist.csv`) has
one row per task for tracking.

---

## 1. Workstream map

| Workstream | Source folder | # tasks | Primary files touched |
|---|---|---|---|
| A. Audit fixes | `team/final-tasks/audit/` | 18 | `distributions.py`, `results.py`, `result.py`, `random_variables.py`, `probability_space.py`, `time_series.py`, `random_processes.py`, `setup.py` |
| B. New/revised distributions | `team/final-tasks/distributions/` | 4 | `distributions.py`, `__init__.py` |
| C. Graphics | `team/final-tasks/graphics/` | 8 patches + 1 infra gap | `plot.py`, `distributions.py` (`Distribution.plot()` only), `results.py` (`RVResults.plot()` dispatch), `result.py` (path-plot methods) |
| D. Display / summarize | `team/final-tasks/summarize-and-misc/` | 2 | `results.py` (`Results.__repr__`/`_repr_html_`) |
| E. Hierarchical models | `team/final-tasks/claude_code_hierarchical_phase1_guide.md` | 1 (already fully scripted) | `probability_space.py`, `__init__.py` |

### File-overlap hot spots (why the phasing below is not just "by workstream")

- **`distributions.py`** — touched by A (Finding 1, Finding 12, fast_sim, random-seed) and B (all four new/revised distributions), plus C's Patch 0001 (but only the shared `Distribution.plot()` base method — inherited automatically by every distribution, not a merge conflict). Research confirmed all these regions are non-adjacent by line number; the only *required* order is inside workstream B (Benford → InverseGaussian → Beta → Rayleigh, since InverseGaussian's insertion point sits immediately before the class Beta's patch modifies).
- **`results.py`** — touched by **A** (Findings 6, 9, 10, 18 — bug fixes to `RVResults.plot()`/`standardize()`), **C** (Patches 0002, 0006, 0007, and the bin-width patch — all add new dispatch branches inside `RVResults.plot()`), and **D** (`Results.__repr__`/`_repr_html_` — a different class than `RVResults`, but the same file). This is the single biggest churn point in the whole batch. Phasing below lands A's bug fixes first, then C's feature patches in their required order, then D last.
- **`result.py`** — touched by **A** (Finding 4, Finding 8, the join/vector-flattening fix — three independent bug fixes to `InfiniteTuple.__getitem__`, `DiscreteTimeFunction.__getitem__`, and `join()`) and **C** (Patches 0005, 0006 — add `style=`/`type=`/`weights=` to the `.plot()` methods on `Tuple`, `InfiniteVector`, `DiscreteTimeFunction`, `ContinuousTimeFunction`). Different methods, but recommend landing A's fixes first so C's feature patches are diffed against the corrected file.
- **`time_series.py`** — Finding 3 (GARCH) and Finding 22 (AR/ARMA docs) sit adjacent in the file and **this adjacency was never verified for apply-conflicts** by the source documents. Apply together, on one branch, and expect to hand-resolve if `git apply` complains.
- **`probability_space.py`** — Finding 14 (BoxModel validation), fast_sim (`ProbabilitySpace.sim`), random-seed (adds `seed()`), and workstream E (adds `HierarchicalProbabilitySpace`, `Hierarchical()`, `__rshift__`) all touch this file in different regions. No confirmed conflict, but it's the second-most-shared file after `results.py` — sequence per the phases below rather than branching all four simultaneously from the same base commit.
- **`plot.py`** — almost entirely a **C** (graphics) file; random-seed's one-line `rng = np.random.default_rng()` → `from .probability_space import rng` swap is the only other touch, and should land early (Phase 3) so the large graphics patches (Phase 5) are diffed against its final state.

---

## 2. Phased plan

Each phase is a set of tasks safe to implement in parallel (on separate
branches) and merge to `dev` in any order *within* the phase. Complete a
phase (merged to `dev`, `pytest tests/` run, `black` applied) before starting
work that the "why" column says depends on it. This ordering blends the
audit's own stated severity ranking with the file-overlap analysis above.

| Phase | Tasks | Why this grouping |
|---|---|---|
| **0** | One-time setup | Environment, branch, remote check — do once, applies to every task below. |
| **1** | Finding 7, Finding 16, Finding 14, Finding 18, Finding 10, Finding 12, Finding 5, Rayleigh scale, Benford's law | Isolated, low-risk, no cross-task file conflicts. Good warm-up tasks; safe to parallelize across multiple people/branches. |
| **2** | Finding 1, Finding 4, Finding 8, join/vector-flattening fix, Finding 3 + Finding 22 (together), InverseGaussian, Beta xmin/xmax | Correctness fixes with silently-wrong-number stakes (per the audit's own priority list) plus the two distribution patches with a required internal order. |
| **3** | Random-seed consolidation (incl. Finding 2), fast_sim performance patch | Infrastructure change to a value (`rng`) referenced almost everywhere; land before further churn in `distributions.py`/`plot.py`/`probability_space.py` so later phases diff against its final state. Fast_sim depends conceptually on random-seed's `rng` consolidation (both reference the same name) though research found it works either order — sequence random-seed first anyway to avoid a two-step migration. |
| **4** | Finding 6, then Finding 9, Finding 22 already done in Phase 2 | Finding 6 **must** land before Finding 9 (both add an identical import line to `test_results.py`; confirmed to conflict in the other order). These prepare `results.py` for the graphics dispatch rewrites in Phase 5. |
| **5** | Graphics patches 0001 → 0002 → 0003 → 0004 → 0005 → 0006 → 0007 → bin_width_and_equal_area, in that exact order | Hard dependency chain (0006 needs 0005's `style=`; 0007 needs 0002's `stackedbar` token; 0003 needs 0001's guard already in `Distribution.plot()`). bin-width patch goes last because it was developed independently against the same base commit and will need manual reconciliation with 0002's `make_hist` title edit and `results.py` docstring changes. |
| **6** | Display-formatting patch, then `.summarize()` (orient-only, no code yet) | `Results.__repr__`/`_repr_html_` is lower-conflict than the `RVResults.plot()` churn above, but sequencing after Phase 5 means it's diffed against `results.py`'s final post-graphics state. `.summarize()`'s design doc explicitly says it should reuse the display-formatting helpers once they exist. |
| **7 (parallel track)** | Hierarchical models Phase 1 | Fully independent feature; only touches `probability_space.py`/`__init__.py` in regions distinct from Phases 1–3's touches to the same file. Can run in parallel with any phase above; the existing guide (`team/final-tasks/claude_code_hierarchical_phase1_guide.md`) is already a complete, ready-to-use script — see Task 34 below, which just points at it. |

Every phase ends the same way, per `CLAUDE.md`:
```
cd symbulate
black .
pytest tests/
```
and every individual task is still its own PR/branch/commit per the
"one PR per task" git convention — "phase" here means *merge-to-`dev`
ordering*, not "one big PR."

---

## 3. Task detail

Each task below: what it does, the Claude Code prompt to hand it, what a
human must verify manually (since no tests were run while producing this
plan), and pitfalls called out in the source documents.

### Phase 0 — One-time setup

Do this once, not per-task:
```bash
git checkout dev && git pull origin dev
```
Confirm environment matches `CLAUDE.md`: Python 3.13, conda env `symbulate`,
`pip install -e .`, `git remote -v` points at `calpoly-symbulate/symbulate`
(not `dlsun/symbulate`). Branch per task: `git checkout -b fix/finding-01-multinomial-float-sum` etc., following `CLAUDE.md`'s `fix/`, `feature/`, `docs/`, `test/` naming.

---

### Phase 1 — Isolated, low-risk fixes

**Task 1 — Finding 7: License mismatch (`setup.py` says GPLv3, `LICENSE.txt` is MIT)**
- File: `setup.py` only (license fields).
- Prompt: *"Read `team/final-tasks/audit/finding07_license_mismatch.patch`. `setup.py` declares `license=\"GPLv3\"` and a matching classifier, but `LICENSE.txt` is the full MIT license text (copyright Dennis Sun and Kevin Ross) — confirm that by reading `LICENSE.txt` yourself first. Change `license=` to `\"MIT\"` and the classifier to `\"License :: OSI Approved :: MIT License\"`. Do not touch the stale `url=`/`author_email=` fields — those are a separate, unbatched audit finding (17), not part of this task."*
- Manual verification: visually confirm `LICENSE.txt`'s actual text before changing anything (no automated test — metadata only).
- Pitfalls: don't also fix `url=`/`author_email=` in the same PR.

**Task 2 — Finding 16: Add `pyproject.toml` / harden `setup.py` packaging**
- Files: new `pyproject.toml`, `setup.py` (non-license fields).
- Prompt: *"Read `team/final-tasks/audit/finding16_pyproject_toml.patch`. Add a `pyproject.toml` with `requires = [\"setuptools>=61\", \"wheel\"]`, `build-backend = \"setuptools.build_meta\"`. Update `setup.py`: `python_requires=\">=3.8\"`, `install_requires=[\"numpy>=1.17\",\"scipy>=1.4\",\"matplotlib>=3.2\"]` (the numpy floor matters because `np.random.default_rng` needs numpy≥1.17, which this package already relies on throughout), and wire `long_description` to read from `README.md` via `Path(__file__).parent / \"README.md\"`. Do not touch the `license=`/classifier fields — that's a separate task (Finding 7)."*
- Manual verification: **not a pytest task** — build in a clean venv, `pip install .`, then `import symbulate; RV(Normal(0,1)).sim(5)` to confirm the package still installs and imports.
- Pitfalls: don't bundle the license fix into this PR.

**Task 3 — Finding 14: `BoxModel` doesn't validate `box`/`size`**
- Files: `symbulate/probability_space.py` (`BoxModel.__init__`), `symbulate/tests/test_probability_space.py`.
- Prompt: *"Read `team/final-tasks/audit/finding14_boxmodel_validation.patch`. `BoxModel.__init__` in `symbulate/probability_space.py` accepts an empty `box` or negative `size` silently, then fails later inside `.draw()` with raw NumPy errors that never mention `box`/`size`. Add two checks right after `self.box` is built, before the existing `probs` length check: raise `ValueError` if `len(self.box) == 0` (say so, per `CLAUDE.md`'s error-message standard); raise `ValueError` if `size` is a real number, not `None`, not `float('inf')`, and `< 0`. Import `numbers` for the type check. Add tests: `BoxModel([])` raises mentioning 'empty'; `BoxModel([1,2,3], size=-2)` raises mentioning 'size'; `BoxModel([1,2,3], size=float('inf'))` **still works** — this is a required regression test, not just a nice-to-have, since `inf` means a legitimate unbounded lazy sequence."*
- Manual verification: confirm `size=None` (draw 1) and `size=float('inf')` are not accidentally rejected.
- Pitfalls: don't reject `size=None` or `size=float('inf')` — only an actual negative number is invalid.

**Task 4 — Finding 18: `results.py` hardcodes a duplicate alpha constant**
- Files: `symbulate/results.py` (import block + one violin-panel line).
- Prompt: *"Read `team/final-tasks/audit/finding18_violin_alpha_constant.patch`. `results.py` has `legacy_alpha = 0.5 if alpha is None else alpha`, re-hardcoding the literal that `plot.py`'s `VIOLIN_ALPHA = 0.5` already defines — `plot.py`'s own comment expects `results.py` to source it from that constant. Import `VIOLIN_ALPHA` from `.plot` and change the line to `VIOLIN_ALPHA if alpha is None else alpha`. This is a behavior-neutral refactor (both currently evaluate to 0.5) — no new test is required, just confirm existing violin-plot tests in `test_results.py` still pass."*
- Manual verification: none beyond existing violin tests passing.
- Pitfalls: none — lowest-risk item in the whole batch.

**Task 5 — Finding 10: `RVResults.standardize()` raises a raw `ZeroDivisionError`**
- Files: `symbulate/results.py` (`standardize()`), `symbulate/tests/test_results.py`.
- Prompt: *"Read `team/final-tasks/audit/finding10_standardize_zerodivision.patch`. `standardize()` computes `(self - self.mean()) / self.std()` with no zero-variance check — a constant `RV` or single-draw simulation raises a raw `ZeroDivisionError`. Compute `std` first, check `np.any(np.asarray(std, dtype=float) == 0)` (use `np.any`, not scalar truthiness — `std` may be array-valued for multi-dimensional results), and raise an explanatory exception (\"Can't standardize data with no variability -- every value is {mean!r}...\") before dividing. Add tests: `RVResults([5.0]*20).standardize()` and `RVResults([3.0]).standardize()` both raise mentioning 'no variability', not `ZeroDivisionError`; normal non-constant data is unaffected."*
- Manual verification: none beyond the new tests.
- Pitfalls: use `np.any(...)`, not a scalar check, since `std` can be array-valued.

**Task 6 — Finding 12: `MultivariateDistribution.corr()` divides by zero silently (with a warning)**
- Files: `symbulate/distributions.py` (`corr()`, ~line 5089), `symbulate/tests/test_distributions.py`.
- Prompt: *"Read `team/final-tasks/audit/finding12_corr_divide_by_zero.patch`. `corr()` computes `cov / np.outer(sd, sd)`, raising a raw, contextless `RuntimeWarning` when any component has zero variance (e.g. `Multinomial(n=10, p=[1,0,0])`). The resulting `nan` is the mathematically correct answer — correlation with a never-varying component is undefined, not zero. Do NOT change the math to avoid the `nan` (e.g. don't return 0). Wrap the division in `with np.errstate(invalid=\"ignore\", divide=\"ignore\"):` and update the docstring to state the `nan` behavior explicitly. Add a test wrapping the call in `warnings.catch_warnings(); warnings.simplefilter(\"error\")` and confirming `Multinomial(n=10, p=[1,0,0]).corr()` computes quietly with every entry `nan`."*
- Manual verification: none beyond the warning-as-error test.
- Pitfalls: do not change the calculation to avoid `nan` — only suppress the warning.

**Task 7 — Finding 5: `RandomProcess.__setitem__` is a silent no-op for bad values**
- Files: `symbulate/random_processes.py` (`__setitem__`), `symbulate/tests/test_random_processes.py`.
- Prompt: *"Read `team/final-tasks/audit/finding05_randomprocess_setitem.patch`. `RandomProcess.__setitem__` has `isinstance(value, RV)` / `elif is_scalar(value)` with no `else` — assigning e.g. a list to `X[0]` silently does nothing (no error, `X.rvs` stays empty). This is the only `__setitem__` in the codebase, so fixing it here covers every subclass. Add `else: raise TypeError(...)` naming both accepted types (RV, scalar) and echoing the rejected value/type, per `CLAUDE.md`'s error-message standard. The existing test `test_non_scalar_non_rv_is_ignored` currently asserts the *broken* behavior — rename it to `test_non_scalar_non_rv_raises_typeerror` and rewrite it to assert `TypeError` is raised, and add a check that the message names both 'RV' and 'scalar'."*
- Manual verification: confirm the old test's assertion (that nothing happens) is fully replaced, not left alongside the new one.
- Pitfalls: don't leave the old "assert the bug" test passing alongside the new one.

**Task 8 — Rayleigh: add `scale` parameter**
- Files: `symbulate/distributions.py` (`Rayleigh`), `symbulate/tests/test_distributions.py`. No `__init__.py` change (already exported).
- Prompt: *"Read `symbulate/distributions.py`'s `HalfNormal.__init__` (it already validates/exposes a `scale` parameter the same way) and the current `Rayleigh` class (just below `Lomax`). Change `Rayleigh()` (currently no arguments, fixed `scale=1`) to `Rayleigh(scale=1.0)`, mirroring `HalfNormal`'s exact structure: validate `scale` is a positive real number via `_validate`, store `self.scale = scale`, pass `params = {\"scale\": scale}` into `super().__init__(params, stats.rayleigh, False)`. Update the docstring with `Parameters`/`Attributes` sections (the current docstring has neither) and one more `Examples` line (`Rayleigh(scale=2).mean()` ≈ 2.5066). Do not add any `xlim` override — support is `(0, inf)` regardless of scale, so the automatic mechanism already handles it. Confirm the default `1.0` exactly reproduces old behavior (existing `TestRayleigh` tests pass unchanged). Add tests: default scale is 1.0, a KS test at a non-default scale against `stats.rayleigh(scale=...)`, mean/variance formulas across several scales, a stretch-identity test (`scale * RV(Rayleigh()) ~ Rayleigh(scale)`), an invalid-scale error case, and a plot smoke test including `.plot(cdf=True)`."*
- Manual verification: confirm `Rayleigh(scale=2).mean() ≈ 2.5066` by hand (`2·√(π/2) = 2.5066`, checks out); spot-check `scale=0.5` and `scale=100` both plot sensibly.
- Pitfalls: none called out — lowest-risk of the four distribution changes.

**Task 9 — Benford's law distribution (new)**
- Files: `symbulate/distributions.py` (new `Benford`/`_benford_gen`, inserted after `Zeta`), `symbulate/__init__.py` (**flag for team sign-off**), `symbulate/tests/test_distributions.py`.
- Prompt: *"Read `symbulate/distributions.py`'s `Makeham` class and its `_makeham_gen` helper, and `Zeta` just above it. Add `Benford(base=10)` — the leading-digit law, pmf `log_base(1 + 1/d)` for `d = 1, ..., base-1`. scipy has no built-in Benford's law, so mirror `_makeham_gen`'s approach exactly: a private `_benford_gen(stats.rv_discrete)` with `_argcheck` (integer `base >= 2`), `_get_support` (returns `(1, base-1)`), and `_pmf`; instantiate once as `_benford = _benford_gen(name=\"benford\")`; then `Benford(Distribution)` validates `base` with `_validate`/`_is_whole_number` and calls `super().__init__({\"base\": self.base}, _benford, True)`. Match `Makeham`/`Zeta`'s docstring shape. Insert directly after `Zeta`, before the `## Continuous Distributions` divider. Do not add a custom `xlim` override — verify `Benford().xlim == (1, 9)` comes from the automatic mechanism. Add `TestBenford` mirroring `TestZeta`'s conventions: pmf-matches-formula, sums-to-one, the ~30.1% leading-1 fact, monotone-decreasing pmf, support bounds, mean ≈3.440237, a `base=2` degenerate case, a chi-square distributional test (seed `distributions.rng`), invalid-base errors, plot smoke test. **Stop before touching `symbulate/__init__.py`** — flag the export for team sign-off per `CLAUDE.md`; don't add it silently."*
- Manual verification: confirm `Benford(base=2)` (support collapses to one point) doesn't break `rv_discrete`'s normalization; confirm the mean constant 3.440237 against an independent source; re-run the chi-square test with a couple of different seeds to rule out a lucky pass.
- Pitfalls: `__init__.py` export needs explicit team sign-off — don't add it as a drive-by.

---

### Phase 2 — Correctness fixes with silently-wrong-number stakes

**Task 10 — Finding 1: `Multinomial`/`NegativeMultinomial` reject valid probability vectors**
- Files: `symbulate/distributions.py` (`Multinomial.__init__` ~6872, `NegativeMultinomial.__init__` ~7341). **No test changes exist in the reference patch — tests must be written from scratch.**
- Prompt: *"Read `team/final-tasks/audit/finding01_multinomial_float_sum.patch`. `Multinomial.__init__` checks `sum(p) == 1` by exact float equality, so `sum([0.4,0.3,0.2,0.1]) == 0.9999999999999999` gets rejected — this is a currently-failing test on `dev` (`test_Multinomial_plot_pairs`), not hypothetical. Fix with `math.isclose(sum(p), 1, abs_tol=1e-9)`. `NegativeMultinomial.__init__` has the mirror bug (`p_arr.sum() >= 1` is boundary-fragile the other direction) — fix with `p_sum > 1 or math.isclose(p_sum, 1, abs_tol=1e-9)` both counting as invalid. Add tests (none exist in the reference patch): `Multinomial(n=12, p=[0.4,0.3,0.2,0.1])` and `Multinomial(n=6, p=[1/6]*6)` construct successfully; `Multinomial(n=5, p=[0.5,0.6])` still raises; `NegativeMultinomial(r=3, p=[1/3,1/3,1/3])` (sum≈1) still raises; `NegativeMultinomial(r=3, p=[0.3,0.2])` still works. Re-run `test_Multinomial_plot_pairs` and confirm it now passes."*
- Manual verification: confirm `test_Multinomial_plot_pairs` (currently failing on `dev`) passes after this fix.
- Pitfalls: none beyond the float-equality trap itself — clean, self-contained fix. Note it shares `distributions.py` with Finding 12 (different region, ~5089 vs ~6872/7341) and with the fast_sim patch (different region again) — no conflict expected but confirm via full-file diff before merging multiple `distributions.py` PRs close together.

**Task 11 — Finding 4: `InfiniteTuple.__getitem__` silently drops the step on open-ended slices**
- Files: `symbulate/result.py` (`InfiniteTuple.__getitem__`, ~709), `symbulate/tests/test_result.py`.
- Prompt: *"Read `team/final-tasks/audit/finding04_infinitetuple_step.patch`. In `InfiniteTuple.__getitem__`, the `n.stop is None` branch does `return type(self)(lambda i: self[i + n.start])` with no `n.step` at all — `iv[2::2]` returns `2,3,4,5,6...` instead of `2,4,6,8...`, and `iv[::2]` returns `self` unchanged. Fix: `start = n.start or 0; step = n.step or 1`; if `start==0 and step==1` return `self` (preserve the identity fast path for `iv[:]`), else `return type(self)(lambda i: self[start + i*step])`. Add tests: `iv[2::2]` → `2,4,6,8,10`; `iv[::2]` → `0,2,4,6,8`; `iv[:]` still `assertIs` the identical object; `iv[2:]` (no step) unchanged. Do not conflate this with Finding 8's separate `RV.__getitem__`/`DiscreteTimeFunction.__getitem__` bug — different classes, different fixes, same file."*
- Manual verification: none beyond the four listed test cases.
- Pitfalls: preserve the `assertIs` identity fast path for bare `iv[:]` while fixing the step bug.

**Task 12 — Finding 8: Open-ended slices crash with a raw `TypeError`**
- Files: `symbulate/random_variables.py` (`RV.__getitem__`, ~241), `symbulate/result.py` (`DiscreteTimeFunction.__getitem__`, ~1127), plus both test files.
- Prompt: *"Read `team/final-tasks/audit/finding08_open_slice_crash.patch`. Two separate `__getitem__` methods build `range(n.start or 0, n.stop, n.step or 1)`, crashing with a raw `TypeError` whenever `n.stop is None` (any open-ended slice, e.g. `X[1:]`). These need genuinely different fixes, not a shared one: (1) `RV.__getitem__` — the realized value `x` already supports native slicing, so replace the range-based reconstruction with `self.apply(lambda x: Vector(x[n]))`, delegating to Python's own slice semantics. (2) `DiscreteTimeFunction.__getitem__` — there is no underlying container to delegate to (the index set is unbounded both directions), so instead raise an explicit `ValueError(\"Cannot slice a DiscreteTimeFunction without a stop index...\")` before hitting `range()`. Add tests: `RV(BoxModel([0,1,2,3,4], size=5))[1:].draw()` returns 4 elements matching native slicing on the *same* underlying draw (draw once, compare `X[1:].func(outcome)` against the full slice — not two independent random draws); `DiscreteTimeFunction(lambda n: n*3, fs=1)[2:]` raises the new `ValueError`; closed slices (`f[0:4]`) are unaffected."*
- Manual verification: none beyond the listed test cases — but double-check the `RV` test compares against the *same* draw, not two independent ones (a subtle but easy mistake that would make the test pass by luck rather than by correctness).
- Pitfalls: don't try to give `DiscreteTimeFunction` the same "delegate to a container" fix as `RV` — there is no container; the fixes must differ.

**Task 13 — Join/vector-flattening fix (unnumbered regression, not an audit finding)**
- Files: `symbulate/result.py` (`join()`), `symbulate/tests/test_random_variables.py`, `symbulate/tests/test_result.py`.
- Prompt: *"Read `team/final-tasks/audit/join_vector_flattening_fix.md` and `join_fix.patch`. `join()` in `symbulate/result.py` uses `isinstance(result1, Tuple)`/`isinstance(result2, Tuple)` to decide whether to flatten a sub-result's `.values` into the joined tuple. Since `Vector` subclasses `Tuple`, this incorrectly flattens any `Vector`-valued component — confirmed via `git bisect` that `main`'s `join()` uses an exact type check (`type(result1) == Tuple`) and `dev` regressed on this in commit `478cf24` (PR #68, no recorded review discussion). `(RV(P) & X & Y).sim(5)` currently produces `(3, 4, 7, 4)` instead of the correct `((3, 4), 7, 4)` when `P` draws a `Vector`. Change both occurrences back to `type(result1) == Tuple` / `type(result2) == Tuple` — this is a revert to known-correct behavior, not new design; do not 'improve' it back to `isinstance` even though that looks more idiomatic. Add a code comment at the fixed line explaining why the exact-type check is deliberate, so a future cleanup pass doesn't reintroduce `isinstance`. Add the four regression tests from the reference patch to `test_result.py`'s `TestJoin` class (including one specifically named to fail if `isinstance` is ever reintroduced) and one to `test_random_variables.py`'s `TestRVJoint` class using `P = DiscreteUniform(a=1,b=4)**2; X=RV(P,sum); Y=RV(P,max); Z=RV(P)&X&Y`, asserting `len(val)==3` and `len(val[0])==2`."*
- Manual verification: the source document reports the **entire `dev` test suite already reruns clean** with this fix (~2,500+ tests, zero failures) — unusually well-verified versus other findings in this batch, but still worth a human re-running the full suite once merged, since this plan did not run it.
- Pitfalls: the original PR that introduced the regression had zero review comments, so the intent behind `isinstance` is *inferred*, not confirmed. Worth a quick check with the original author before merging the revert, in case there's an undocumented reason — though the `main`-branch comparison strongly suggests it was accidental.

**Task 14 — Finding 3: GARCH/ARCH stationary-variance formula silently wrong for non-unit-variance noise**
- Files: `symbulate/time_series.py` (`_garch_is_stationary`, `_unconditional_variance`, `_validate_garch`, `GARCHProbabilitySpace.__init__`), `symbulate/tests/test_time_series.py`.
- **Apply together with Task 15 (Finding 22) on the same branch** — both touch `time_series.py` in adjacent, previously-unverified-for-conflicts regions.
- Prompt: *"Read `team/final-tasks/audit/symbulate_garch_variance_fix.patch`. `_garch_is_stationary` and `_unconditional_variance` in `symbulate/time_series.py` assume `noise_dist` has variance 1 and never validate it — confirmed numerically: `GARCH(omega=0.2, arch_coefs=[0.1], garch_coefs=[0.85], noise_dist=Normal(0,2))` reports 'stationary' but true persistence is `0.1*2 + 0.85 = 1.05 > 1`, genuinely explosive (simulated variance at t=300 reaches ~6×10²⁸). Add a `noise_var` parameter to both functions: `_garch_is_stationary` returns `sum(arch_coefs)*noise_var + sum(garch_coefs) < 1`; `_unconditional_variance` returns `omega / (1 - sum(arch_coefs)*noise_var - sum(garch_coefs))`. Update the two call sites (`_validate_garch`'s `initial='stationary'` check, `GARCHProbabilitySpace.__init__`) to pass `float(noise_dist.var())`, and update the `ValueError` message and `GARCH` docstring to describe 'persistence' rather than a bare coefficient sum. Do not touch `AR`/`ARMA`/`MA` in the same file in this task — that's Finding 22, being done alongside this on the same branch; keep the two diffs distinguishable in your commits. Add regression tests: a case where `arch_coefs+garch_coefs<1` but `noise_var` pushes true persistence over 1 shows growing variance (`X[60].sim(3000).var() > 2*X[5].sim(3000).var()`), a closed-form match at moderate `n` (not `n=0` — see pitfall below), and a test that `initial='stationary'` now correctly rejects a case the old check would have passed."*
- Manual verification: the reference patch's tests deliberately avoid `n=0` because of a separate, pre-existing warm-up transient (the pre-time-0 stand-in uses `initial` for both the squared-value slot and the variance slot, only equal when `noise_var==1`) — confirm this is understood/acceptable, not itself worth a follow-up bug report.
- Pitfalls: don't assume `noise_var=1` anywhere in the formula; the closed-form test must wait for the warm-up transient to decay (test at `n=20,40`, not `n=0`).

**Task 15 — Finding 22: `AR`/`ARMA`'s documented mean formula breaks for nonzero-mean noise (docs-only)**
- Files: `symbulate/time_series.py` (docstrings only — `MA`, `ARMA`, `AR`'s "Notes" section), `symbulate/tests/test_time_series.py`.
- **Apply on the same branch as Task 14.**
- Prompt: *"Read `team/final-tasks/audit/finding22_ar_arma_mean_docs.patch`. `AR`/`ARMA`/`MA` docstrings claim `mean` \"really is the mean of every value\" for a stationary process, but this breaks for nonzero-mean noise: `AR(coefs=[0.5], noise_dist=Normal(mean=2,sd=1), mean=0)` actually simulates to ~4.0, not 0. True formula (verify by simulation): `mean + noise_dist.mean()*(1+sum(ma_coefs))/(1-sum(ar_coefs))` for AR/ARMA, `mean + noise_dist.mean()*(1+sum(coefs))` for MA. This is a **documentation-only fix** — do NOT implement a code fix that centers shocks to make `mean` literally correct everywhere; that would silently change several existing doctest outputs elsewhere in this file that deliberately use `noise_dist=Bernoulli(1)` to demonstrate recursion mechanics with predictable arithmetic (e.g. `path[0]==1.0, path[1]==1.5` would become `0.0`), which is a bigger, separate design decision requiring its own team sign-off — not part of this task. Before touching anything, run `pytest --doctest-modules symbulate/time_series.py` to see the current passing baseline, update only the `mean` parameter docs in `MA`, `ARMA`, and `AR`'s own separate 'Notes' copy of the claim, then rerun the same doctest command and confirm nothing that was passing now fails. Add documentation-pinning tests: simulate `AR`, `ARMA`, `MA` each with nonzero-mean `noise_dist` and confirm simulated mean matches the stated formula within simulation tolerance — these exist specifically to catch anyone who changes the recursion later without updating docs to match."*
- Manual verification: confirm via `pytest --doctest-modules symbulate/time_series.py` before and after that no existing doctest (especially the `Bernoulli(1)`-based ones) breaks.
- Pitfalls: do **not** implement the "real" code fix (centering shocks) here — flagged explicitly as a separate, bigger follow-up needing its own design decision.

**Task 16 — InverseGaussian/Wald distribution (new)**
- Files: `symbulate/distributions.py` (new `InverseGaussian`, inserted after `LogGamma`, before `Beta`), `symbulate/__init__.py` (**flag for team sign-off**), `symbulate/tests/test_distributions.py`.
- **Apply before Task 17 (Beta xmin/xmax)** — Beta's patch diff context assumes this insertion already happened.
- Prompt: *"Read `symbulate/distributions.py`'s `LogGamma` class (just above where Beta begins) and `HalfNormal`/`HalfCauchy` for docstring/validation style. Add `InverseGaussian(mean=1.0, shape=1.0)` directly after `LogGamma` and before `Beta`, wrapping `scipy.stats.invgauss`. Parameterize by the textbook `(mean, shape)` convention, not scipy's own `mu`/`scale`: translate with `mu = mean / shape`, `scale = shape` — put this derivation in a code comment so a future reader doesn't have to re-derive it. Validate both parameters are positive real numbers with `_validate`. Store the raw mean parameter as `self.mean_param` — **not** `self.mean`, since `Distribution.__init__` assigns `self.mean` to the callable that returns the expected value; explain this naming collision in an inline comment. Add docstring sections matching `LogGamma`'s style, including that variance is `mean**3 / shape` and the distribution approaches `Normal(mean, mean**3/shape)` as `shape → infinity`. Do not touch `xlim` — confirm the automatic mechanism gives `(0, quantile(0.999))` since `invgauss`'s support is `(0, inf)`. Add `TestInverseGaussian`: a KS test against `stats.invgauss(mu=mean/shape, scale=shape)`, mean/variance formula checks across several `(mean, shape)` pairs, pdf agreement with scipy under the translated parameters, the shape→∞ normal-limit check, invalid-parameter errors, plot smoke test. **Stop before touching `symbulate/__init__.py`** — flag the export for team sign-off."*
- Manual verification: confirm `InverseGaussian(mean=2, shape=3).mean() == 2.0` and `.var() == 2.6667` numerically in a REPL; confirm the pdf integrates to 1 via `scipy.integrate.quad` for at least one `(mean, shape)` pair; grep the codebase for any place that introspects `.mean` as a stored attribute rather than calling it (would silently miss `mean_param`); check the shape→∞ test isn't flaky at `shape=1_000_000`.
- Pitfalls: "despite the name, it is unrelated to the reciprocal of a normal random variable" — keep that note in the docstring so students aren't misled. `__init__.py` export needs sign-off.

**Task 17 — Beta: add `xmin`/`xmax` parameters**
- Files: `symbulate/distributions.py` (`Beta.__init__`), `symbulate/tests/test_distributions.py`. No `__init__.py` change (already exported).
- **Apply after Task 16.** Highest regression risk in the distributions workstream — `Beta` is reused internally by `PERT` and `Dirichlet`.
- Prompt: *"Read the current `Beta` class and `PERT.__init__` (just below Beta — PERT already stretches a beta via `loc`/`scale` internally). Change `Beta.__init__` to `def __init__(self, shape1, shape2, xmin=0.0, xmax=1.0)`. Pass `loc=xmin, scale=xmax-xmin` into the existing `params` dict alongside `a=shape1, b=shape2` — reuse `PERT`'s existing mechanism, don't invent a new one. Validate with `_validate`: both real numbers, `xmax` strictly greater than `xmin`, but only run the ordering check once both are confirmed numeric (guard with an `isinstance` check first, matching `_validate`'s own documented convention for cross-parameter comparisons) so a bad type doesn't throw a raw `TypeError` inside the check itself. Store `self.xmin`/`self.xmax`. Update the docstring: `xmin`/`xmax` in `Parameters`/`Attributes`, a `Notes` section on the stretch-and-shift identity, one more `Examples` line (`Beta(1, 1, xmin=2, xmax=4).mean() == 3.0`). Confirm the defaults exactly reproduce old behavior. Confirm `Dirichlet`'s internal `Beta(shape1=a_i, shape2=self.alpha0 - a_i)` call (search `distributions.py` for it) is unaffected — it never passes `xmin`/`xmax`. Do not touch `xlim` — verify `Beta(2, 5, xmin=10, xmax=20)._support()` returns `(10.0, 20.0)` automatically. Append tests to the existing `TestBeta` class: default-bounds-are-(0,1), a KS test comparing against a hand-stretched `RV(Beta(a,b))`, a mean-formula check, a `_support()` check, error cases (`xmax<=xmin`, non-numeric bounds), a plot smoke test with non-default bounds. **Run the entire existing `TestBeta`, `TestPERT`, and `TestDirichlet` classes afterward** (not just new tests) to confirm nothing regressed."*
- Manual verification: **must** run the full pre-existing `TestBeta`/`TestPERT`/`TestDirichlet` suites, not just the new tests — this is explicitly the change most likely to have a hidden regression, precisely because `Beta` is reused internally. Spot-check a very small positive width (`xmax - xmin = 1e-10`) doesn't cause numerical issues. Hand-verify the doctest `Beta(1,1,xmin=2,xmax=4).mean()==3.0` (Beta(1,1) is Uniform(0,1), mean 0.5, stretched: `2+2*0.5=3` ✓).
- Pitfalls: this is the single biggest regression-risk item in the distributions workstream. Don't skip the `PERT`/`Dirichlet` regression run.

---

### Phase 3 — Seeding & performance infrastructure

**Task 18 — Random-seed consolidation (includes Finding 2's docstring fix)**
- Files: `probability_space.py` (new `seed()`), six other modules' `rng = np.random.default_rng()` line each (`diffusion_process.py`, `distributions.py`, `gaussian_process.py`, `hitting_times.py`, `markov_chains.py`, `plot.py`), `random_variables.py` (two docstring examples), `__init__.py` (**flag for team sign-off** — new public function), ~11 test files.
- Prompt: *"Read `team/final-tasks/audit/random_seed_notes.md` in full. Symbulate has seven independent, unconnected `np.random.default_rng()` instances (one per module), and `np.random.seed()` does nothing to any of them — the `RV.draw()` docstring in `random_variables.py` currently claims a specific reproducible value that reseeding does not actually produce; this is Finding 2, fixed as part of this task. **Before implementing, flag explicitly to the team/reviewer that this adds a new public function (`seed()`) exported from `symbulate/__init__.py`** — `CLAUDE.md` requires flagging any `__init__.py`/public-API change; do not bundle this silently into a routine PR. Make `probability_space.py` the sole owner of `rng` (nearly everything already imports from it). Add `def seed(value=None): rng.bit_generator.state = np.random.default_rng(value).bit_generator.state` there — **mutate the existing generator's state in place, do not rebind the name** (`rng = new_generator` would break the `from .probability_space import rng` link the other six modules rely on; mutating `.bit_generator.state` correctly propagates because `from X import rng` binds every module's `rng` name to the same object). Change the other six modules' `rng = np.random.default_rng()` line to `from .probability_space import rng`. Export `seed` from `symbulate/__init__.py`. Fix the two misleading `np.random.seed`-based docstring examples in `random_variables.py` — regenerate the actual values by running the fixed code, don't guess them; mark `# doctest: +SKIP` if an exact value would be a maintenance burden. Double-check there is no circular import (`probability_space.py` must not import from any of the six modules that will import `rng` from it — flagged as unverified in the source notes). Update the ~11 test files currently monkey-patching each module's private `rng` with hand-picked offset seeds (including `test_merton.py`, which has a confirmed **dead line**, `renewal_process.rng = ...`, since `renewal_process.py` has no `rng` of its own) to call the new `seed(...)` instead, deleting each file's bespoke local `seed()` helper. Add a regression test: `seed(42)`, draw from several different distributions/processes (a `Normal`, a `MarkovChain`, a `DiffusionProcess`), reseed with the same value, confirm the second run matches the first. Add a test that `seed()` with no argument still produces an unpredictable run."*
- Manual verification: confirm no circular import results (source notes explicitly flag this as unverified); confirm the regenerated docstring values in `random_variables.py` are real outputs, not guessed.
- Pitfalls: `__init__.py` export needs explicit sign-off — this is a genuine new public API surface, not a bugfix. Rebind-vs-mutate is the crux of the whole fix; get it backwards and the six modules silently keep using disconnected generators.

**Task 19 — Fast_sim performance patch (`NegativeHypergeometric`, `TruncatedNormal`)**
- Files: `distributions.py` (new `Distribution._fast_sim` hook + two overrides), `probability_space.py` (`ProbabilitySpace.sim`), `random_variables.py` (new `_IDENTITY` sentinel, `RV.sim` fast path).
- **Land after Task 18** (references the same `rng` name — works either order per the source notes, but sequencing avoids a two-step migration).
- Note: `fast_sim_patch.py` is an explicit **draft sketch**, not an applyable diff (`"""DRAFT PATCH -- not meant to be imported or run as-is."""`) — Claude Code must translate its five labeled edits into the real classes, not `git apply` it.
- Prompt: *"Read `team/final-tasks/audit/fast_sim_notes.md` and `fast_sim_patch.py` in full — the latter is an explicit draft sketch, not a real diff, so translate its five labeled edits into the actual files, don't try to apply it. `NegativeHypergeometric` (backed by `scipy.stats.nhypergeom`) rebuilds an entire CDF table + inverse-CDF interpolator from scratch on every single `.draw()` call — measured ~850x slower looped vs. batched. `TruncatedNormal` similarly redoes real per-call work — ~80x slower looped. Add a `_fast_sim(self, n)` hook to the base `Distribution` class (returning `None` by default) right after the `xlim` setter. Add one-line overrides on `NegativeHypergeometric` and `TruncatedNormal`: `return self.sim_func(**self.params, size=n, random_state=rng)`, using the module's existing `rng` — **do not add an `rng` parameter to `_fast_sim`**; the notes file explains why (two historically independent `rng` instances in this codebase — see Task 18 — and a passed-in `rng` risks silently advancing the wrong stream). Add a guarded fast path to `ProbabilitySpace.sim()`: call `self._fast_sim(n)` if present, fall through to the existing `_sim_with_progress` loop when it returns `None`. In `random_variables.py`, add a named `_IDENTITY = lambda x: x` sentinel used as `RV.__init__`'s default `func=` value; guard `RV.sim()`'s fast path on `self.func is _IDENTITY and hasattr(self.prob_space, '_fast_sim')` so any `.apply()`-transformed or conditioned RV always takes the untouched slow path. Must not change results for any distribution other than these two, and must not change `.pdf`/`.cdf`/`.mean` for these two either — only `.sim(n)` on a bare, unmodified instance. Add tests: confirm `_fast_sim` is `None` for other distributions; a KS-test statistical-equivalence check between looped and batched output; a generous-margin timing regression test (e.g. 10,000 `NegativeHypergeometric` draws under 1 second); confirm `X.apply(lambda x: x)` still takes the slow path (its lambda is a *different* object than the `_IDENTITY` sentinel, so `is` correctly fails)."*
- Manual verification: **flag to a human reviewer explicitly** that the *sequence* of values for a fixed seed changes for these two distributions specifically (scipy's batched `rvs(size=n)` doesn't draw bits in the same order as `n` sequential calls) — any existing test or notebook hardcoding an exact sequence for these two distributions needs regenerating.
- Pitfalls: don't add an `rng` parameter to `_fast_sim`. Don't let the `_IDENTITY is` check accidentally catch a hand-written `.apply(lambda x: x)` — verify with an explicit test, not just by reading the code.

---

### Phase 4 — `results.py` dispatch bug fixes (prep for graphics phase)

**Task 20 — Finding 6: Inconsistent handling of bad plot kwargs**
- Files: `symbulate/results.py` (`RVResults.plot()`), `symbulate/tests/test_results.py`.
- **Must merge before Task 21 (Finding 9)** — both patches add an identical `matplotlib.use("Agg")` import block to `test_results.py`; applying Finding 9 after Finding 6 in the wrong order was confirmed to produce a duplicate-import conflict.
- Prompt: *"Read `team/final-tasks/audit/finding06_plot_kwargs.patch`. Two problems in `RVResults.plot()`: (1) `make_rug` (1D branch) and `make_segmented_rug` (2D branch) never forward `**kwargs`, unlike every sibling branch — `type='rug', linewidth=10` is silently dropped. (2) A bad kwarg on `type='hist'` raises a raw `AttributeError` straight from matplotlib (violates `CLAUDE.md`'s error standard), while `type='box'`/`type='violin'` raise `TypeError` for the identical mistake — inconsistent exception types across plot types, verified empirically (matplotlib's Artist-forwarding calls raise `AttributeError`, not `TypeError`, so a fix catching only `TypeError` would miss most of these). Add `_call_plot_helper(func, *args, **kwargs)` near the top of `results.py` that catches both `TypeError` and `AttributeError`; if `\"unexpected keyword argument\"` is in the message, re-raise a friendly `TypeError` naming the likely cause, otherwise re-raise unchanged (don't mislabel a genuine bug inside a `make_*` helper as a kwarg problem). Route the ~9 `make_*` calls in the 1D branch through it. Extend the same wrapping to the ~18 calls in the 2D/categorical branches (scatter, hist2d, density2d, tile, mosaic, segmented_hist, segmented_density, segmented_rug, categorical bar/dotplot/impulse) as part of this same PR — there are no reference tests for this sweep, write them from scratch and manually verify each of the ~18 call sites still behaves normally for its ordinary usage. Add tests: `type='rug', linewidth=10` actually applies; bad kwargs on `type='rug'`, `type='hist'`, `type='box'` all raise the same friendly `TypeError`; normal usage of every plot type is unaffected."*
- Manual verification: the ~18-call-site follow-up sweep has zero reference tests — a human needs to actually render each affected plot type once with normal kwargs to confirm nothing broke, since this plan did not run anything.
- Pitfalls: catching only `TypeError` misses most real cases (empirically verified). Don't mislabel a genuine internal bug as a kwarg-typo message.

**Task 21 — Finding 9: `dims=` validated after the pairs-matrix has already partially drawn**
- Files: `symbulate/results.py` (`RVResults.plot()`), `symbulate/tests/test_results.py`.
- **Merge after Task 20.**
- Prompt: *"Read `team/final-tasks/audit/finding09_dims_validation_order.patch`. `RVResults.plot()` checks `if \"dims\" in kwargs: raise ValueError(...)` *after* the `self.dim is not None and self.dim > 2` branch, but that branch can return early for the `\"pairs\"` case — building a `GridSpec`, resizing the figure, adding a subplot — before the `dims=` check ever runs, leaving a half-built figure behind even though it eventually raises. Move the `dims=` check up to immediately after the existing `if \"pairs\" in kwargs` check, before the `self.dim > 2` branch begins; delete the old dead copy further down. Add a test: call `.plot(dims=[0,1])` on a 3-variable `RVResults`, confirm `ValueError` mentioning `dims=`, and confirm `plt.gcf().get_axes()` is empty afterward — proving nothing was drawn before the check ran."*
- Manual verification: none beyond the listed test.
- Pitfalls: none beyond the merge-order note above.

---

### Phase 5 — Graphics patches (strict order)

All eight items in this phase have a verified dependency chain: **0001 → 0002 → 0003 → 0004 → 0005 → 0006 → 0007 → bin_width_and_equal_area**. Apply in exactly this order. `symbulate-graphics-changes-summary.md` reports 0001–0007 verified against `dev@dce6e10` with 832/832 passing in `test_plot.py`+`test_result.py`+`test_results.py` and 921/922 in `test_distributions.py` (one pre-existing, unrelated failure — the same Multinomial float-equality bug fixed in Task 10). This plan did not re-run those numbers.

**Task 22 — Patch 0001: Guard degenerate distribution params and pdf/cdf overlay**
- Files: `symbulate/distributions.py` (`Distribution.plot()`), `symbulate/plot.py` (new `THEORETICAL_CDF_PDF_OVERLAY_ERROR` constant), `symbulate/tests/test_distributions.py`.
- Prompt: *"Read `team/final-tasks/graphics/0001-fix-guard-degenerate-distribution-params-and-pdf-cdf.patch` and `team/final-tasks/graphics/symbulate_graphics_testing_strategy.md`'s 'Layer 3' section — the patch is the concrete implementation of that document's Guard 1 and Guard 2. Implement both in `Distribution.plot()`: (1) right after `ys = self.cdf(xs) if cdf else self.pdf(xs)`, raise a `ValueError` naming the distribution and plotting window when every value is non-finite (e.g. `Normal(mean=0,sd=0)`, `Uniform(a=5,b=5)`, `Gamma(shape=1e-6,rate=1)`) — don't let `ys[np.isfinite(ys)].max()` reach a bare NumPy 'zero-size array' crash. (2) Add `THEORETICAL_CDF_PDF_OVERLAY_ERROR` next to the existing `MARGINAL_OVERLAY_ERROR` in `plot.py`; tag the axes with `ax._symbulate_theoretical_kind` (`\"cdf\"` or `\"pdf/pmf\"`) so a second, mismatched call on the same axes raises — this is a hard error, the strictest overlay tier; **stop and confirm with the team before merging as a hard error rather than a warning**, per the testing-strategy doc's own note that this is a product decision belonging in `DECISIONS.md`, not something to decide unilaterally. (3) Change title-setting from always-overwrite to keep-first, matching the existing label-setting convention; rename default titles: 'PDF Plot' → 'Probability Density Function', 'CDF Plot' → 'Cumulative Distribution Function', 'PMF Plot' → 'Probability Mass Function'. Update `test_distributions.py`'s many `test_*_plots_without_error` methods that call both `.plot()` and `.plot(cdf=True)` on a shared axes — isolate them with `plt.figure()` between calls, since the new overlay guard will otherwise make them raise. Add regression tests for both guards (the four degenerate-parameter repro cases above; same-kind overlay still works, mismatched kind raises, `ax.twinx()` is a documented escape hatch)."*
- Manual verification: render the four degenerate-parameter repro cases in a notebook to confirm the friendly message appears instead of the raw NumPy error; confirm `Normal(0,1).plot(); Normal(0,1).plot(cdf=True)` now raises with a readable message and that `ax.twinx()` is a working escape hatch; visually confirm the keep-first-title change doesn't break a pdf-over-histogram overlay (histogram's title should survive).
- Pitfalls: **team decision needed** — hard error vs. warning for the overlay guard (see Section 4 below).

**Task 23 — Patch 0002: Rename simulated/theoretical plot titles; mosaic/stackedbar split; boxplot/violin outlier defaults**
- Files: `symbulate/plot.py` (nearly every `make_*` title, `_mosaic_spans`, new constants), `symbulate/results.py` (2D dispatch, `equal_width` removal). Highest-conflict-risk single patch in the whole batch — touches ~15 functions.
- **Apply after Task 22.**
- Prompt: *"Read `team/final-tasks/graphics/0002-feat-rename-simulated-theoretical-plot-titles-to-ful.patch` and `symbulate-graphics-changes-summary.md`'s '0002' section in full before writing any code. Apply only after Patch 0001 is in. Implement three independent changes as one coherent PR: (1) Title renames across `plot.py` — update `PLOT_DISPLAY_NAME` and the titles in `make_tile`, `make_impulse`, `make_hist`, `make_density`, `make_segmented_density`, `make_segmented_hist`, `make_hist2d`, `make_density2D`, `make_joint_pdf`, `make_joint_pmf` per the patch's mapping (e.g. 'Joint Histogram' replaces both '2-D Histogram' and the old tile-histogram name). Update the now-stale `CLAUDE.md` paragraph under '2D density surfaces are banded by default' to match — don't leave the doc out of sync with the code. (2) Flip `make_boxplot`'s and `make_grouped_boxplot`'s `outliers` default `True`→`False`; add a matching `outliers=` parameter to `make_violinplot`. (3) The mosaic/stackedbar split: flip `annotate`/`marginal_column` defaults to `False` in `make_mosaic`; fix the `_mosaic_spans` trailing-gap bug (a zero-weight segment must not reserve space — only nonzero-weight segments count toward the gap budget, so drawn segments always sum to exactly the total); split `type=\"mosaic\"` (proportional widths) and `type=\"stackedbar\"` (equal widths) as two distinct tokens in `RVResults.plot()`, removing `equal_width=` entirely and raising a clear error pointing at `type=\"stackedbar\"` if passed; change mosaic overlay from a printed warning to a hard `ValueError` (`MOSAIC_OVERLAY_ERROR`); add a many-categories suggestion nudge via a new named constant `MOSAIC_MANY_CATEGORIES_THRESHOLD = 4` (don't hardcode 4 inline, per `CLAUDE.md`'s named-constant rule). **Before implementing the mosaic/stackedbar mapping, stop and confirm with the team explicitly**: should `type=\"mosaic\"` mean proportional widths (the patch author's choice, matching pre-existing title logic and textbook terminology) or equal widths? Do not guess and proceed — flagged as unresolved in the source patch. Do not touch `Distribution.plot()`'s titles (already done by 0001) or anything in `distributions.py`."*
- Manual verification: heaviest visual-verification item in the batch. Render a mosaic with a zero-count segment and confirm bars reach exactly 0/1 (the `_mosaic_spans` fix). Render `type=\"mosaic\"` and `type=\"stackedbar\"` side by side on the same data and confirm segment heights match while widths differ. Render a boxplot on data with a known outlier under both `outliers=True` and the new `False` default and confirm whisker extents. Capture stdout and confirm suggestion-note text for `density2d`/`hist2d`/mosaic/stackedbar reads correctly end-to-end.
- Pitfalls: **two team decisions needed** — the `equal_width`→mosaic/stackedbar mapping (above), and note this patch textually conflicts with the bin-width patch (last in this phase) at `make_hist`'s title line and in `results.py`'s docstring — expect manual reconciliation there, not a clean apply.

**Task 24 — Patch 0003: Pad discrete distribution plot axes**
- Files: `symbulate/distributions.py` (`Distribution.plot()`, same method Task 22 also modifies).
- **Apply after Task 22** (same method body — expect line-context shifts if reordered).
- Prompt: *"Read `team/final-tasks/graphics/0003-fix-pad-discrete-distribution-plot-axes-so-points-ar.patch`. Apply after Patch 0001. Add one whole-number slot of view padding on each side of a discrete distribution's resolved `xlim` window, matching the existing 'one slot' convention `configure_axes` already uses for simulated impulse/dot plots — don't invent a new padding constant, reuse that existing amount (1 unit). Track the true (unpadded) window as `ax._symbulate_true_xlim` so overlaying a second discrete distribution compares against the real data window, not the already-padded view — otherwise padding compounds on every overlaid call. Continuous distributions and the actually-evaluated `xs` array must be completely unaffected — only the axes' view widens. Add regression tests: a discrete pmf's/cdf's leftmost/rightmost point sits strictly inside the view (not on the spine); a continuous distribution's view is unchanged; two overlaid discrete `.plot()` calls with the same window don't accumulate padding across calls."*
- Manual verification: render `Poisson(3).plot()` and `.plot(cdf=True)` and visually confirm air around the leftmost/rightmost point (compare against the original screenshot complaint this patch was written to fix); render two overlaid discrete distributions and confirm padding doesn't visibly grow with each call.
- Pitfalls: omitting `_symbulate_true_xlim` and reading `ax.get_xlim()` directly instead is exactly the bug this patch avoids — don't reintroduce it.

**Task 25 — Patch 0004: Rotate right-hand marginal strip's frequency tick labels**
- Files: `symbulate/plot.py` (`thin_marginal_frequency_ticks`, new `MARGINAL_FREQ_TICK_ROTATION` constant). Independent of every other graphics patch — can technically apply anytime, kept in sequence here for simplicity.
- Prompt: *"Read `team/final-tasks/graphics/0004-fix-rotate-right-hand-marginal-strip-s-frequency-tic.patch`. Add `MARGINAL_FREQ_TICK_ROTATION = 90` next to the existing `MARGINAL_FREQ_TICKS` constant. In `thin_marginal_frequency_ticks(marg_ax, orientation, integer=False)`, when `orientation == \"horizontal\"` (the right-hand strip), rotate that axis's tick labels by `MARGINAL_FREQ_TICK_ROTATION` degrees with alignment set so labels read cleanly; leave `orientation == \"vertical\"` (top strip) untouched. Do not touch the `_symbulate_freq_ticks` recording mechanism this function already uses for dot-plot relayout — it must keep working exactly as documented in `CLAUDE.md`. This function is shared by both `RVResults.plot(marginal=True)` and `MultivariateDistribution.plot()` — verify the fix by rendering both. Add tests: right strip's labels are rotated, top strip's are not; rendered label bounding boxes don't overlap (`Text.get_window_extent`), not just checking the rotation angle."*
- Manual verification: purely visual — render `RV(Normal(0,1)**2).sim(500).plot(marginal=True)` and a `MultivariateNormal` joint plot, zoom into the right strip, confirm decimal frequency labels no longer overlap. The patch documents a known accepted residual (a possible sub-pixel overlap even at 90°) — confirm it's still sub-pixel/invisible at normal zoom, don't chase it further via `GridSpec wspace` adjustments (explicitly flagged as higher-risk than it's worth).
- Pitfalls: don't "fix" the documented sub-pixel residual further — it's a deliberate stopping point, not an oversight.

**Task 26 — Patch 0005: Render sample paths by their actual shape (dots, steps, line)**
- Files: `symbulate/plot.py` (`make_sample_path`), `symbulate/result.py` (`Tuple.plot()`, `InfiniteVector.plot()`, `DiscreteTimeFunction.plot()`, `ContinuousTimeFunction.plot()`, new `_discrete_valued_step_path` helper).
- **Hard prerequisite for Task 27 (0006).**
- Prompt: *"Read `team/final-tasks/graphics/0005-feat-render-sample-paths-by-their-actual-shape-dots-.patch`, and read `CLAUDE.md`'s 'Continuous-Time, Discrete-State Processes — Required Interface' section (the documented gap for `NonHomogeneousPoissonProcess`/`CoxProcess`) before touching anything. Add `style=\"line\"` to `make_sample_path()`: `\"line\"` (today's default, unchanged), `\"dots\"` (marker + dashed line), `\"steps\"` (`drawstyle=\"steps-post\"`); raise `ValueError` naming the three options for anything else. Do not touch the existing whole-number-tick logic already in this function — `CLAUDE.md` explicitly forbids forcing whole-number ticks unconditionally; that logic must stay exactly as-is, placed after the new style handling. In `result.py`: default `Tuple.plot()`, `InfiniteVector.plot()`, `DiscreteTimeFunction.plot()` to `style=\"dots\"` via `kwargs.setdefault`. Add `_discrete_valued_step_path(discrete_valued, tmin, tmax)`, walking `get_states()`/`get_arrival_times()` (the `states[n]` held for `interarrival_times[n]` convention `CLAUDE.md` already documents) to build exact step data. In `ContinuousTimeFunction.plot()`: check `isinstance(self, DiscreteValued)`; if true, try `_discrete_valued_step_path` and default to `style=\"steps\"`; catch `AttributeError` specifically (the documented `NonHomogeneousPoissonProcess`/`CoxProcess` gap — do not try to fix that gap here, fall back gracefully to the existing dense 200-point grid). Every non-`DiscreteValued` continuous path is unchanged. Add tests: `style=` directly in `test_plot.py`; each Result type's default style, exact step data at various windows including one starting mid-state, the smooth-path no-op case, and the `AttributeError` fallback (using a fake `DiscreteValued` subclass defining only `get_states()`) in `test_result.py`."*
- Manual verification: render `MM1(...).sim(1).plot()` or `PoissonProcess(rate=2).sim(1).plot()` and confirm sharp exact jumps replace the old fine-grid approximation; render a `Tuple`/`InfiniteVector`/`DiscreteTimeFunction` path and confirm dots+dashes; confirm `BrownianMotion(...).sim(1).plot()` is visually unchanged; specifically test a `CoxProcess`/`NonHomogeneousPoissonProcess` path falls back silently rather than raising.
- Pitfalls: the `NonHomogeneousPoissonProcess`/`CoxProcess` gap is deliberately not fixed here. `SIR`/`SEIR` per-compartment views (`path.S`, `path.I`) are deliberately **not** given step-accurate rendering — a known, deliberate limitation, not an oversight to fix in review.

**Task 27 — Patch 0006: Histogram/impulse plots of a jump process's visited states**
- Files: `symbulate/result.py` (`InfiniteVector.plot()` gains `type=`/`weights=`, `DiscreteValued.num_jumps_by`, new `_reject_path_plot_type` helper on `Tuple`/`DiscreteTimeFunction`/`ContinuousTimeFunction`), `symbulate/plot.py` (new `weighted_count_var`, `weights=` on `make_impulse`/`make_hist`), `symbulate/results.py` (forward `type="hist"`/`"impulse"` for path-shaped results).
- **Hard dependency on Task 26 (0005)** — this patch's diff is written against 0005's already-modified `result.py`.
- Prompt: *"Read `team/final-tasks/graphics/0006-feat-histogram-impulse-plots-of-a-jump-process-s-vis.patch` and `symbulate_states_hist_impulse_plan.md` in full — the plan doc is the design rationale, treat this patch as its direct implementation. Apply only after Patch 0005. Implement: (1) `InfiniteVector.plot()` gains `type=None, weights=None`; `type in (\"hist\", \"impulse\")` draws `make_hist`/`make_impulse` on `self[tmin:tmax]`; any other non-`None` type raises `ValueError` naming the two valid options. (2) Add `weighted_count_var(x, weights)` to `plot.py` next to the existing `count_var`; add `weights=` to `make_impulse` and `make_hist` (the latter passes straight through to `ax.hist(weights=...)`). (3) Add `num_jumps_by(t)` to the `DiscreteValued` mixin, walking `get_arrival_times()` the same way `ContinuousTimeMarkovChainResult` already evaluates itself at a given time — reuse that pattern. (4) Add `_reject_path_plot_type(type, plot_description)` and call it from `Tuple.plot()`, `DiscreteTimeFunction.plot()`, `ContinuousTimeFunction.plot()` to reject any non-`None` `type=` with a clear error — only `InfiniteVector.plot()` actually supports `type=`. (5) In `RVResults.plot()`'s final path-plot branch (`dim is None`), forward `type` to each realization's `.plot()` only when it's `\"hist\"` or `\"impulse\"` — leave every other value (including `\"path\"`) unforwarded, exactly as the patch does. Do not change the `dim>2` pairs-matrix-is-default behavior — this only affects `dim is None` results. Add tests: `type=` dispatch and error cases in `test_result.py`; `weights=` on `make_hist`/`make_impulse` directly in `test_plot.py`; `num_jumps_by` correctness including the 'counts jumps up to and including t' boundary case."*
- Manual verification: render `path = ContinuousTimeMarkovChain(...).draw(); path.states.plot(tmax=100, type=\"impulse\")` and the weighted version (`weights=path.interarrival_times`) side by side and confirm the unweighted (embedded-chain) and time-weighted distributions visibly differ when holding times are state-dependent — this distinction is the entire point and can't be validated by a unit test alone. Confirm `X.apply(states).sim(n).plot(type=\"impulse\")` works via the `RVResults.plot()` forwarding path.
- Pitfalls: a per-jump count and a time-weighted count answer genuinely different questions — this must be documented, not just implemented correctly, so a student doesn't assume the naive per-jump histogram is a time-average. Deliberately not implemented: `type=` on a *time-indexed* states histogram for `DiscreteTimeFunction`/`ContinuousTimeFunction` (sampling on a grid only approximates a time-weighted histogram) — don't add this as a "nice to have."

**Task 28 — Patch 0007: Dispatch two dependent categorical variables to a real 2D plot**
- Files: `symbulate/results.py` (new `_is_categorical_2d`, new dispatch branch in `RVResults.plot()`).
- **Depends on Task 23 (0002)'s `stackedbar` token existing.** Sits near Task 27 (0006)'s edits in the same dispatch cascade — apply in this stated order (0006 then 0007).
- Prompt: *"Read `team/final-tasks/graphics/0007-fix-dispatch-two-dependent-categorical-variables-to-.patch` and `symbulate-graphics-changes-summary.md`'s '0007' section for the exact repro case. Apply only after Patches 0002 and 0006 are in `results.py`. `(X & Y).sim(1000).plot()` for two dependent categorical (string) variables built from a raw `ProbabilitySpace` currently falls through every numeric and 1D-categorical check into the generic ensemble path-plot branch, drawing a meaningless two-point 'Sample Path' — because `RVResults.dim` is only computed for numeric results. Add `_is_categorical_2d(results)` mirroring the existing `_is_categorical_1d`. Add a new dispatch branch, checked before the existing `_is_categorical_1d` branch, routing such pairs to `type=\"tile\"` (default), `\"mosaic\"`, or `\"stackedbar\"` — all three already accept raw categorical arrays directly (confirmed by reading `make_tile`/`make_mosaic`'s existing signatures). Raise a clear error for `type=\"scatter\"` (categorical scatter isn't supported — `make_scatter`'s jitter modes assume integer-coded positions) and for `marginal=True` (untested against string axes) rather than letting either crash obscurely inside matplotlib. Do not implement a categorical scatter plot or extend `make_scatter`'s jitter modes — out of scope. Whether `\"mosaic\"` should be the default instead of `\"tile\"` is an **explicitly open, unresolved team question** raised in the original bug report — implement `\"tile\"` as the default per the patch's conservative choice, and flag the alternative to the team as a one-line change if they prefer mosaic; do not resolve this unilaterally. Add tests using the exact `event_sim` repro from the bug report (two `BoxModel`s, one conditional on the other): default is a real 2D plot (not 'Sample Path'), explicit `type=\"mosaic\"`/`\"stackedbar\"` both work, tile labels use actual category strings, small-n still defaults to tile, both `type=\"scatter\"` and `marginal=True` raise clear errors."*
- Manual verification: run the exact `event_sim` example from the bug report and confirm `.plot()` now shows a real tile plot with category-name labels instead of "Sample Path."
- Pitfalls: **team decision needed** — tile vs. mosaic as the default for two categorical variables (see Section 4).

**Task 29 — bin_width_and_equal_area.patch: `bin_width=`/`equal_area=` on `RVResults.plot(type="hist")`**
- Files: `symbulate/plot.py` (new `resolve_hist_bins`, inserted before `make_hist`), `symbulate/results.py` (`RVResults.plot()` signature/docstring/1D-hist dispatch).
- **Apply last in this phase** — developed independently against the same base commit as 0001–0007; expect to manually reconcile against Task 23 (0002)'s `make_hist` title edit and `results.py` docstring changes.
- Prompt: *"Read `team/final-tasks/graphics/histogram-binning-conversation-summary.md` and `bin_width_and_equal_area.patch` in full. Implement `bin_width=`/`equal_area=` for `RVResults.plot()`, scoped to single-variable `type=\"hist\"` only. Add `resolve_hist_bins(values, bins=None, bin_width=None, equal_area=False, default_bins=None)` to `plot.py` immediately before `make_hist`, using the existing `HIST_DEFAULT_BINS` constant as fallback (don't redefine it). Precedence, matching the existing 'explicit `bins=` always wins' rule already in `DECISIONS.md`: an explicit edges array always wins → `bin_width`/`equal_area` are mutually exclusive (`equal_area` wins on conflict) → `bin_width` converts to a bin count via `ceil(range/bin_width)` → `equal_area` places edges at quantile points, deduping via `np.unique`. Wire into the 1D-hist dispatch only. Add three `warnings.warn` guards matching the existing pattern used for `bins`-on-dotplot: used without `type=\"hist\"`, used on a 2D plot, used on the pairs matrix. Do not touch `hist2d`, `segmented_hist`, `tile`, or `setup_tile_axis` — `setup_tile_axis` does `bins + 1` int arithmetic that would break with an edges array; that's explicitly out of scope. Since this patch was developed independently against the same base commit as the 0001–0007 graphics patches, **expect a manual merge conflict** at `make_hist`'s title-setting line (renamed by Patch 0002) and in `RVResults.plot()`'s docstring/`Parameters` block (also touched by 0002) — resolve by keeping 0002's title rename and layering this patch's new parameters/warnings on top, not by reverting either patch's changes. Add tests: bin count from `bin_width`; `bin_width<=0` raises; each conflict warns and produces the documented winner; equal-area bins are approximately equal-area but not equal-width; the duplicate-edge collapse case; 2D/pairs plots warn rather than silently ignoring the kwargs."*
- Manual verification: render `RV(Normal(0,1)).sim(2000).plot(type=\"hist\", bin_width=0.25)` and `.plot(type=\"hist\", bins=30, equal_area=True)` and visually confirm the equal-area histogram is narrow in the dense middle, wide in the tails — a property a unit test checking `area≈target` can't fully substitute for. Confirm two team decisions before merging (see Section 4): does `bin_width` really beat `bins` on conflict, or should it raise like `tabulate()` does; and `bin_width` vs. `binwidth` naming to match `tabulate()`.
- Pitfalls: expect a real merge conflict with Task 23, not a clean apply. Two pre-existing, unrelated flaky tests (`TestPlot2DMosaic::test_mosaic_marginal_column_has_no_in_cell_labels`, `TestPlot2DSegmentedDensity::test_segmented_density_discrete_x_continuous_y`) were observed to flake in the full suite due to unseeded `np.random` in `setUp` — confirmed unrelated to this patch (reproduces on unmodified `dev`), but worth a separate quick fix (seed `np.random` in their `setUp` like the rest of the file does).

**Task 30 — Testing-infrastructure gap: build the missing regression sweep + blank-plot detector**
- Not a patch — a gap identified by `symbulate_graphics_testing_strategy.md`. The doc's "Layer 2" describes a `test_plot_regression_sweep.py` as "delivered alongside this document," but **it does not exist in this folder** — confirmed by directory listing. Treat as missing, not complete.
- Files: new `symbulate/tests/test_plot_regression_sweep.py` (Layer 2), a new `axes_is_blank(ax, ...)` pixel-based helper (Layer 1) — location TBD, likely `symbulate/tests/` or a test-only helper module.
- Prompt: *"Read `team/final-tasks/graphics/symbulate_graphics_testing_strategy.md` in full. Its 'Layer 2' section describes a table-driven `test_plot_regression_sweep.py` iterating over all `Distribution.__subclasses__()` (the doc reports 55 found) with a `REGISTRY` of minimal constructor kwargs, a `SKIPPED` dict with reasons (e.g. `Hotelling`/`Wishart`/`InverseWishart`), a build-failing test if any subclass falls into neither dict, and a sweep asserting `.plot()`/`.plot(cdf=True)` never raise, warn, or render blank — but this file does not exist anywhere in the repo despite the doc's claim it was 'delivered alongside this document.' Build it now, following the doc's exact design. Also build 'Layer 1': a generic `axes_is_blank(ax, shrink=0.06, pixel_tol=8, area_tol=0.0005)` helper that renders the figure and inspects actual pixels rather than checking `ax.has_data()`/artist counts (both demonstrated in the doc to be unreliable — e.g. `ax.plot([], [])` reports `has_data()==True` but is visually blank). Validate it against the doc's own table of synthetic blank/non-blank cases before using it in the sweep. Flag that this helper is tuned against default matplotlib figure size/DPI and may need recalibration if `symbulate.mplstyle`'s figure size differs."*
- Manual verification: after building, run the sweep once across all distributions and confirm the reported baseline (0 failures across 104 calls, per the doc) is reproduced — flag any new failure as a genuine regression from Phase 5's changes, not a flaky test.
- Pitfalls: don't skip the "recalibration against `symbulate.mplstyle`" step — `pixel_tol`/`area_tol` were tuned against a specific figure size in the doc's own environment, not necessarily this project's default. Also worth a near-term follow-on (not required for this batch): the doc notes there is **no CI workflow at all** in the repo (`.github/workflows/` doesn't exist) — recommend a bare-bones `pytest` GitHub Actions workflow as separate, lower-priority follow-up work.

---

### Phase 6 — Display formatting & `.summarize()`

**Task 31 — Display-formatting patch (`Results.__repr__`/`_repr_html_`)**
- Files: `symbulate/results.py` (`Results` class — not `RVResults` — new module constant `RESULT_DISPLAY_DECIMALS`, new helpers `_format_scalar`/`_format_result`/`_format_display_column`, new `_display_rows()`, rewritten `__repr__`/`_repr_html_`).
- Ready to apply as-is (confirmed not yet applied; confirmed against current `results.py` imports).
- Prompt: *"Read `team/final-tasks/summarize-and-misc/display-formatting-changes.md` and `results_display_formatting.patch` in full. This is a ready-made, already-reviewed unified diff — your job is to apply it to `symbulate/results.py`, not redesign it. Confirm first by reading the current file that `Results.__repr__`, `Results._repr_html_`, and the existing `is_number`/`is_numeric_vector` imports from `.result` match what the patch's context lines expect (they do). Apply it — do not 'improve' on it: no `decimals=` kwarg, no scientific-notation fallback, both explicitly out of scope per the doc. Then: (1) run `pytest tests/test_results.py -v`, confirm all pass including the `TestResultsRepr` class; (2) run `pytest --doctest-modules symbulate/results.py`, confirm only the pre-existing, unrelated `Results.filter` doctest failure remains (don't fix that one — it predates this change and isn't part of this task); (3) add new tests for what the doc's manual smoke-testing covered but automated tests don't: an empty `Results` (n=0), a single-value `Results` (n=1), a numeric-vector column with *inconsistent* component dimension across rows (confirm `_format_display_column`'s fallback branch doesn't crash), and exactly 10 and exactly 11 rows for both `__repr__` and `_repr_html_` (regression test for the old `_repr_html_` bug — dropped row 9 at n=11, spurious ellipsis at n=10/11). Do not touch `symbulate/table.py` — the doc explicitly marks `Table`'s display as out of scope."*
- Manual verification: check `_repr_html_` rendering in an actual Jupyter notebook cell (right-aligned CSS, not just terminal `repr()`); confirm vector outcomes with inconsistent dimension across rows degrade sensibly rather than looking broken (no example of this shown in the doc).
- Pitfalls: floats always round to `RESULT_DISPLAY_DECIMALS=4` with no per-call override — this is a known, accepted limitation, not a bug to fix. No scientific-notation fallback means `1e-8` silently prints `0.0000` — flagged as a known gap; watch for it if `.summarize()` (Task 32) later surfaces extreme-magnitude distributions.

**Task 32 — `.summarize()` method — orient only, do NOT implement yet**
- Files: none yet — `summarize-method-design.md` is a design sketch, confirmed **not implemented** (`symbulate/results.py` has no `summarize`/`crosstab` today).
- This is not implementation-ready. The design doc itself lists five open questions, two of which (two-way table API shape; whether `.plot()` fires as a side effect) block half the feature's signature. Follow the "orient, no code yet" pattern from the hierarchical-models guide, not a direct-implementation prompt.
- Prompt: *"Read `team/final-tasks/summarize-and-misc/summarize-method-design.md` in full — a design sketch for `RVResults.summarize()`, explicitly not final; it lists five open questions near the end. Also read the existing `tabulate()` method(s) in `symbulate/results.py` and `symbulate/base.py`'s `Statistical` mixin, to confirm the doc's claims about what already exists (`.mean()`, `.std()`, `.quantile(q)` — single `q` only, `.corr()`, etc.) are still accurate. Also check whether `symbulate/plot.py`'s live `classify_values` function accepts a `large_n_rescue=` keyword — the design doc's proposed dispatch code calls `classify_values(..., large_n_rescue=False)`, which does not appear to exist in the current signature (`classify_values(values, n_unique_threshold=B_1D, n_small_threshold=N_SMALL_THRESHOLD)`) — confirm or correct this discrepancy. **Do NOT write any implementation code yet.** Instead: (1) recommend an answer to each of the doc's five open questions (two-way table API shape — new `tabulate(twoway=True)` kwarg vs. a dedicated `.crosstab()` method; whether `.plot()` fires as a side effect vs. just being described, with reasoning tied to `CLAUDE.md`'s Overlay Policy precedent for keeping side effects opt-in; return type — a `Summary` object parallel to `Table`, a plain dict, or both; whether `.summarize()`'s tables should reuse the `RESULT_DISPLAY_DECIMALS` constant from Task 31 or take their own precision; how to handle mixed discrete/continuous pairs and 3+ dimensions); (2) resolve the `classify_values` discrepancy; (3) propose a phased implementation plan (the doc itself suggests Phase 1 = 1D discrete + 1D continuous, Phase 2 = 2D discrete + 2D continuous with the two-way table, Phase 3 = polish/shared `Summary` object — say if you'd order differently and why). I want to review and answer the open questions before any code is written."*
- Only after this orient step comes back with team-approved answers should a second, direct-implementation prompt be issued for whichever phase is approved first (mirroring "Prompt 2" in the hierarchical-models guide).
- Manual verification (once eventually implemented): unit tests per case (1D discrete, 1D continuous, 2D discrete, 2D continuous) plus named edge cases (empty results, `dim` mismatches, mixed discrete/continuous pairs raising `NotImplementedError` with an actionable message per `CLAUDE.md`'s error standard, `dim>2` raising `ValueError`); manual visual check of the printed table and `_repr_html_` for all four cases, especially the two-way table's header alignment; a consistency check between the proposed `np.quantile` multi-quantile bypass and `.quantile()`'s existing single-`q` values; and — given the known scientific-notation gap from Task 31 — an explicit test with something like `Exponential(rate=1e6)` to see if `.summarize()` silently prints `0.0000` for tiny values.
- Pitfalls: this is fundamentally blocked on team decisions, not on missing code-writing effort — resist the temptation to have Claude Code "just pick something reasonable" for the two-way table API or the plotting side effect; both are real product decisions per the design doc's own framing.

---

### Phase 7 (parallel track) — Hierarchical models Phase 1

**Task 33 — hierarchical `>>` operator**
- A complete, ready-to-use, already-written guide exists: `team/final-tasks/claude_code_hierarchical_phase1_guide.md`. It is a full 10-prompt script (orient → implement → export → tests → doctest polish → decision-log update → self-review diff → commit → PR → respond-to-review), already grounded in this repo's actual file paths and conventions. **Do not re-derive this plan — use the guide directly.**
- Files: `symbulate/probability_space.py` (new `HierarchicalProbabilitySpace`, `Hierarchical()`, `ProbabilitySpace.__rshift__`), `symbulate/__init__.py` (**flag for team sign-off**, per the guide's own Section 4), `symbulate/tests/test_probability_space.py`, `MODEL-DECISIONS.md`.
- This can run in parallel with any phase above — it's a fully independent feature. It shares `probability_space.py` with Task 3 (Finding 14, `BoxModel` validation), Task 18 (random-seed's `seed()` function), and Task 19 (fast_sim's `ProbabilitySpace.sim` change) — different classes/methods in each case (no confirmed conflict), but if developed on a long-lived branch in parallel with Phases 1–3, expect to rebase onto `dev` before merging rather than assuming a clean fast-forward.
- Prompt: use the guide's own "Prompt 1" through "Prompt 10" verbatim, in order — see `team/final-tasks/claude_code_hierarchical_phase1_guide.md`.
- Manual verification: the guide's own Section 6 ("Manual smoke test — don't skip this") — run the Beta-Binomial, Gamma-Poisson, discrete-mixture, arbitrary-depth-chain, and `TypeError`-guard examples verbatim in a REPL, since automated tests confirm numeric closeness but not that the `>>` syntax reads the way the design doc promised.
- Pitfalls: all called out in the guide's own "Troubleshooting" section — most notably, if Claude Code proposes touching `distributions.py`, `__pow__`, or `__mul__`, that's scope creep; push back. `symbulate/__init__.py` export needs the same team-flagging discipline as every other public-API addition in this plan.

---

## 4. Open team decisions (do not let Claude Code decide these alone)

Five places across this batch where a patch author explicitly left a product
decision unmade. Resolve these *before* or *during* the relevant task, not
after — several block half a task's implementation.

1. **Overlay hard-error tier** (Task 22 / Patch 0001) — is a mismatched pdf/cdf overlay a hard error (as implemented) or should it be a softer warning, matching the "readability warning" tier used elsewhere in the Overlay Policy? The testing-strategy doc frames this as belonging in `DECISIONS.md`.
2. **`equal_width` → `mosaic`/`stackedbar` mapping** (Task 23 / Patch 0002) — does `type="mosaic"` mean proportional widths or equal widths? The patch author picked proportional (matching pre-existing title logic and textbook usage) but flagged it as unconfirmed.
3. **Default plot type for two dependent categorical variables** (Task 28 / Patch 0007) — `"tile"` (implemented, conservative) or `"mosaic"` (raised as a question in the original bug report)?
4. **`bin_width` conflict/naming** (Task 29) — should `bin_width` win over an explicit `bins=` on conflict (as implemented), or should it raise like `tabulate()` does? Should it be renamed `binwidth` to match `tabulate()`'s existing spelling?
5. **`.summarize()`'s five open questions** (Task 32) — two-way table API shape, whether `.plot()` is a side effect, return type, precision constant reuse, and mixed/3+-dim handling. All five are genuinely unresolved; see Task 32's prompt for the full list.

Also two new **public API additions** needing explicit sign-off per `CLAUDE.md` (not blocking, but must not be added silently):
- `seed()` exported from `symbulate/__init__.py` (Task 18).
- `Benford`, `InverseGaussian` exported from `symbulate/__init__.py` (Tasks 9, 16).
- `Hierarchical`, `HierarchicalProbabilitySpace` exported from `symbulate/__init__.py` (Task 33 — already flagged in the existing guide).

---

## 5. Testing gaps not covered by any patch (plan for separately)

- **No CI at all.** `symbulate_graphics_testing_strategy.md` confirms there is no `.github/workflows/` in this repo — nothing in this entire batch runs automatically on a PR. Worth a bare-bones `pytest` GitHub Actions workflow as follow-up, independent of any task above.
- **The promised `test_plot_regression_sweep.py` (graphics testing "Layer 2") does not exist** despite one source doc claiming it was "delivered alongside" it — see Task 30, which now covers building it.
- **The pixel-based blank-plot detector ("Layer 1")** is also not yet built — same task.
- **Two pre-existing flaky tests** (`TestPlot2DMosaic::test_mosaic_marginal_column_has_no_in_cell_labels`, `TestPlot2DSegmentedDensity::test_segmented_density_discrete_x_continuous_y`) flake in the full suite due to unseeded `np.random` in their own `setUp` — confirmed to reproduce on an unmodified `dev` checkout, unrelated to any patch in this batch, but worth a quick independent fix (seed `np.random` in `setUp` like the rest of the file already does).
- **Findings 11, 13, 15, 17, 19–21, 23–25** from the original audit have no patches in this batch. Out of scope for this plan, but the audit document (`symbulate_dev_audit_2026-08.md`) still describes them — worth a separate pass if the team wants full audit coverage.
- **The join/vector-flattening fix's origin PR had zero review** — worth a quick conversation with whoever wrote the original `isinstance` change before merging the revert (Task 13), even though the bisect evidence strongly supports it being accidental.
- **Full-suite re-runs are owed after every phase**, not just per-task — this plan is based on document inspection and dry-run diff checks, not on running anything. In particular, `pytest tests/` should be run fresh after each phase in Section 2, and especially after Phase 5 (graphics), which is reported to have been checked at 832/832 and 921/922 against a slightly earlier `dev` commit than the one currently on this branch.
